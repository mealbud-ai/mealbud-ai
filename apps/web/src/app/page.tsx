import { Button } from '@repo/ui/components/button';

export default function Home() {
  return (
    <div className="flex h-screen w-screen items-center justify-center bg-background text-primary-foreground">
      <Button>Start Building Now</Button>
    </div>
  );
}
